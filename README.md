# 🏭 Marwan Hub Factories v3.0.0

نظام إدارة المصانع الذكية المتكامل - بني بلغة Rust و Python

## 🌟 المميزات الرئيسية

### ✅ نظام متكامل
- 4 أنواع مصانع (تعليم، إبداع، شركات، تقنية)
- نظام MH-OS v2.2 الذكي للإدارة
- واجهات متعددة (API، CLI، ويب)

### ✅ واجهات مستخدم متقدمة
- لوحة تحكم مباشرة
- لوحة إدارة متقدمة
- نظام تصدير وتنزيل كامل

### ✅ دعم نشر متعدد
- Docker محلي
- Kubernetes
- منصات مجانية (Railway، Render، Fly.io)
- Termux للأندرويد

## 🚀 البدء السريع

### التشغيل المحلي
```bash
# 1. استنساخ المشروع
git clone https://github.com/marwanhubeg/factories.git
cd factories

# 2. تشغيل النظام
./start_unified_system.sh

# 3. افتح المتصفح
#    · http://localhost:8000 - الواجهة الرئيسية
#    · http://localhost:8000/admin_dashboard.html - لوحة الإدارة
الإدارة
```

بيانات تسجيل الدخول الافتراضية

```
· المستخدم: admin / admin123
· المستخدم: user / user123
```

📦 النشر على منصات مجانية

Railway (يوصى به - سهل وسريع)

```bash
# 1. تثبيت Railway CLI
npm i -g @railway/cli

# 2. تسجيل الدخول
railway login

# 3. نشر المشروع
railway up
```

Render

```bash
# 1. إنشاء حساب على render.com
# 2. رفع المشروع إلى GitHub
# 3. ربط المستودع مع Render
# 4. سيقوم Render بالبناء والنشر تلقائياً
```

Fly.io

```bash
# 1. تثبيت Fly CLI
curl -L https://fly.io/install.sh | sh

# 2. تسجيل الدخول
fly auth login

# 3. نشر المشروع
fly launch
```

🏗️ هيكل المشروع

```
marwan-hub-factories-v3.0.0/
├── src/                    # كود Rust الأساسي
│   ├── core/              # المكونات الأساسية
│   ├── factories/         # أنواع المصانع
│   ├── api/               # واجهة API
│   └── mhos/              # نظام MH-OS الذكي
├── templates/             # القوالب الجاهزة
├── products/              # المنتجات الناتجة
├── exports/               # الملفات المصدرة
├── unified_server_final.py # الخادم الموحد
├── database.py            # نظام قاعدة البيانات
├── auth_system.py         # نظام المصادقة
├── admin_dashboard.html   # لوحة الإدارة
└── Dockerfile.final       # تكوين Docker للنشر
```

🔧 واجهة API

```http
GET    /api/factories       # حالة المصانع
GET    /api/products        # قائمة المنتجات
GET    /api/exports         # الملفات المصدرة
POST   /api/factories/start # بدء تشغيل مصنع
POST   /api/export          # تصدير المنتجات
POST   /api/auth/login      # تسجيل الدخول
```

📊 الروابط النشطة بعد النشر

Railway

```
· الواجهة الرئيسية: https://marwan-hub-factories.up.railway.app
· لوحة الإدارة: https://marwan-hub-factories.up.railway.app/admin_dashboard.html
· API: https://marwan-hub-factories.up.railway.app/api/factories
```

Render

```
· الواجهة الرئيسية: https://marwan-hub-factories.onrender.com
```

Fly.io

```
· الواجهة الرئيسية: https://marwan-hub-factories.fly.dev
```

🤝 المساهمة

1. Fork المشروع
2. إنشاء فرع للميزة (git checkout -b feature/AmazingFeature)
3. Commit التغييرات (git commit -m 'Add AmazingFeature')
4. Push إلى الفرع (git push origin feature/AmazingFeature)
5. فتح Pull Request

📄 الرخصة

هذا المشروع مرخص تحت رخصة MIT - انظر ملف LICENSE للتفاصيل.

👥 المؤلفون

· Marwan Hub - المطور الرئيسي
· المساهمون

📞 الدعم

· 📧 البريد: support@marwanhub.com
· 🐛 المشاكل: GitHub Issues
· 💬 النقاشات: GitHub Discussions

---

⭐ إذا أعجبك المشروع، لا تنسى إعطاء نجمة على GitHub!
