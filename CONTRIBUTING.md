# 👋 مساهمة في Marwan Hub Factories

شكراً لاهتمامك بالمساهمة في Marwan Hub Factories! هذا المشروع مفتوح المصدر ونحن نرحب بجميع المساهمات.

## 📋 طرق المساهمة

### 1. الإبلاغ عن الأخطاء
- استخدم [نموذج الإبلاغ عن الأخطاء](.github/ISSUE_TEMPLATE/bug_report.md)
- تأكد من أن الخطأ لم يتم الإبلاغ عنه مسبقاً
- قدم وصفاً مفصلاً بما في ذلك خطوات إعادة الإنتاج

### 2. اقتراح ميزات جديدة
- استخدم [نموذج طلب الميزة](.github/ISSUE_TEMPLATE/feature_request.md)
- اشرح المشكلة التي تحلها الميزة
- قدم أمثلة على كيفية استخدام الميزة

### 3. إرسال طلبات السحب (Pull Requests)
1. انسخ المشروع (`git fork`)
2. أنشئ فرعاً جديداً (`git checkout -b feature/ميزة-جديدة`)
3. أضف تغييراتك (`git commit -m 'إضافة ميزة جديدة'`)
4. ادفع الفرع (`git push origin feature/ميزة-جديدة`)
5. أنشئ طلب سحب

## 🔧 بيئة التطوير

### المتطلبات الأساسية
```bash
# Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# الأدوات الإضافية
cargo install cargo-watch
cargo install cargo-tarpaulin  # للاختبارات
# استنساخ المشروع
git clone https://github.com/marwan-hub/factories.git
cd factories

# بناء المشروع
cargo build

# تشغيل الاختبارات
cargo test

# تشغيل التطبيق
cargo run -- serve
# جميع الاختبارات
cargo test

# اختبارات الوحدات
cargo test --lib

# اختبارات التكامل
cargo test --test '*'

# اختبارات الأداء
cargo bench

# تغطية الكود
cargo tarpaulin --ignore-tests
